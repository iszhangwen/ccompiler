# 后端的主要工作是：代码生成，机器有关优化

## 代码生成

### 指令选择算法：

（1）宏展开是一种简单的模板匹配方法，将每条IR直接替换为对应的机器指令。这种方法实现简单，但无法处理复杂的N:1或N:M映射，效率较低。

（2）树覆盖通过将IR和机器指令表示为树结构，利用模式匹配覆盖整个IR语法树。它解决了宏展开的局限性，但对多输出场景支持不足。

（3）DAG覆盖改进了树覆盖，将数据结构扩展为有向无环图（DAG）。它能够处理多输出边的情况，是现代编译器中广泛采用的模式。

### 寄存器分配分解为两个子问题：

（1）分配：对于